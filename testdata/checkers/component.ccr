component(
  name = "ls",
  deps = [
    ":ls_bin",
  ],
  chks = [
    "common://checks:noop",
  ],
)

resource(
  name   = "ls_bin",
  parent = "common://resources:binary",
  path   = "/bin/ls",
  source = file('ls'),
)

resource(
  name   = "goody",
  parent = "common://resources:file",
  path   = "bantz",
  source = file('ls'),
)
